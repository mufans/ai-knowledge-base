name: Daily Knowledge Collection

on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  collect:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: v2/requirements.txt

      - name: Install dependencies
        working-directory: v2
        run: pip install -r requirements.txt

      - name: Run pipeline
        working-directory: v2
        env:
          LLM_PROVIDER: ${{ secrets.LLM_PROVIDER }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          DASHSCOPE_API_KEY: ${{ secrets.DASHSCOPE_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python pipeline/pipeline.py --sources github,rss --limit 20 --verbose

      - name: Validate JSON
        working-directory: v2
        run: python hooks/validate_json.py knowledge/articles/*.json

      - name: Check quality
        working-directory: v2
        run: python hooks/check_quality.py knowledge/articles/*.json

      - name: Count and commit
        id: count
        run: |
          DATE=$(date -u +%Y-%m-%d)
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add v2/knowledge/
          COUNT=$(git diff --cached --name-only --diff-filter=A -- 'v2/knowledge/articles/*.json' | wc -l | tr -d ' ')
          echo "count=$COUNT" >> "$GITHUB_OUTPUT"
          echo "New articles: $COUNT"
          if [ "$COUNT" -gt 0 ]; then
            git commit -m "docs: add ${DATE} collection — ${COUNT} articles"
            git push
          else
            echo "No new articles, skipping commit."
          fi